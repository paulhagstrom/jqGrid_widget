<%= html_table_to_wire(@jqgrid_id) %>
<%= wire_jqgrid(@jqgrid_id,
	:caption => 'Placeholder',
	:height => 50,
	:enable_panel => false,
	# To enable an edit panel under the titlebar:
	# :enable_panel => 'title',
	# :sortname => 'created_at',
	:pager => nil,
	:columns => @columns
	)
%>
